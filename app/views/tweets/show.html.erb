   <%= render partial: "hedaer",locals:{ tweet: @tweet}%>

    <div class="content">
         <%= image_tag @tweet.image, class: "show-item-img" if @tweet.image.attached? %> 
         <h1 class="card-title"> <%= @tweet.title%> </h1>
        
         <p class="card-text">食材: <%= @tweet.catch_copy%></p>

      <br>   
         <p class="card-text">■作り方■
          <div class="retern">
           <%= @tweet.recipe%></p>
          </div>
　　<div class="card-link">
         <a href="https://twitter.com/share?url=#{request.url}","><i class="fab fa-twitter"></i>twitter</a>
         <br>
         <i class="fas fa-trash-alt"></i><%= link_to "削除",  tweet_path(@tweet), method: :delete %>
         <i class="fas fa-wrench"></i><%= link_to "編集",  edit_tweet_path(@tweet[:id])%>
　　</div>
    </div>

